<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg　navar-dark bg-dark">
        <div class="container">
        <%= link_to "Bookers", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler"></button>
        <li>
          <%= link_to 'Home', root_path, class: "fas fa-home nav-link text-light" %>
        </li>
          </li>

      <% if user_signed_in? %>
        <li>
      <%= link_to 'Users', users_path, class: "fas fa-users nav-link text-light" %>
        </li>
      <% else %>
        <li>
      <%= link_to 'About', a_path, class: "fas fa-link nav-link text-light" %>
        </li>
      <% end %>

      <% if user_signed_in? %>

        <li>
      <%= link_to 'Books', books_path, class: "fas fa-book-open nav-link text-light" %>
        </li>
      <% else %>
        <li>
      <%= link_to 'Sign up', new_user_registration_path, class: "fas fa-user-plus nav-link text-light" %>
        </li>
      <% end %>

        <% if user_signed_in? %>
        <li>
        <%= link_to "Log out", destroy_user_session_path, class: "fas fa-sign-out-alt nav-link text-light", method: :delete %>
     <% else %>
        <li>
      <%= link_to 'Log in', new_user_session_path, class: "fas fa-sign-in-alt nav-link text-light" %>
        </li>
      <% end %>
      </div>
      </nav>
    </header>



    <%= yield %>
<footer>
<div class="container mt-5">
  <div class="row">
  <div class="mx-auto">
    <p>CopyRight Infratop.inc</p>
  </div>
  </div>
</div>
</footer>
  </body>
</html>
